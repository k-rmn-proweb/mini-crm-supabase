# Supabase — значения из Project Settings → API
# Anon key публичный (безопасность через RLS). Service role key НИКОГДА не класть сюда.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
